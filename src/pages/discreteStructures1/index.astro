---
import ClassHomePage from '@layouts/ClassHomePage.astro';
import type { CollectionEntry } from 'astro:content';
import { getCollection } from 'astro:content';

// Fetch all blog posts
const allPosts: CollectionEntry<'discreteStructures1'>[] = (await getCollection('discreteStructures1')).filter((post) => post.data.draft === false);


export async function getStaticPaths() {
  const allPosts: CollectionEntry<'discreteStructures1'>[] = (await getCollection('discreteStructures1')).filter((post) => post.data.draft === false);

  return allPosts.map(entry => ({
    params: {
      slug: entry.slug
    }
  }));
}

const allArticles = allPosts;
const folderPath1 = "../toolbox";
const folderPath2 = "../rankings";
const folderPath3 = "../ratings";
const pathHome =  './';

---

<ClassHomePage
  folderPath1= {folderPath1} 
  folderPath2 = {folderPath2} 
  folderPath3 = {folderPath3} 
  pathHome = {pathHome} 
  pageTitle="Discrete Structures 1 Directory" 
  allArticles = {allArticles} 
  className = "discreteStructures1" 
  fullClassTitle = "Discrete Structures 1 01:198:205">
</ClassHomePage>
